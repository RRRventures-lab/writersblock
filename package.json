{
  "name": "comedy-social-platform",
  "version": "1.0.0",
  "description": "AI-powered comedy social media platform with personalized recommendations",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "deploy:do": "./deploy/deploy.sh",
    "setup:do": "./deploy/digitalocean-setup.sh",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test && cd ../frontend && npm test"
  },
  "keywords": ["social-media", "comedy", "ai", "recommendation", "fullstack"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}